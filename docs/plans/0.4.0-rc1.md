# 0.4.0-rc1 — next named snapshot

Fill this in. It is the intended contents of the next tag after
`v0.3.0-rc1`. Move a line here when you decide it belongs in that
release; move landed work into [`CHANGELOG.md`](../../CHANGELOG.md)
**Unreleased** as it hits `main`.

**Job:** thicker `0.x` minor — features + M3-driven fixes + maintenance
on `main`. Not an rc2 of 0.3.0.

**Tag when:** tests green, this list and the changelog agree, working
tree clean. M3 does not have to be done.

**Then:** `v1.0.0` only after M3 is archived and D2 (auto-merge) is
written. That 1.0 tree may be this 0.4 product.

Version rules: [`next-chapters.md`](next-chapters.md) § Versioning.
Release checklist: [`docs/release-review.md`](../release-review.md).

---

## Features

New operator-facing capability. Add bullets you want in the tag.

-

## Fixes

Bugs found on rc1, during M3, or on `main`. Prefer one regression test
per protocol bug.

-

## Maintenance

Docs, craft, slop, rename/clarify. No new surface.

-

## Out (not this tag)

- **M3 still unverified** until archived under `test/fixtures/m3/`
- **`v1.0.0`** — official usable; not this number
- **`v0.3.0-rc2`** — only if you later cut a *thin* patch of rc1 with
  no new product. Default is to skip it and ride fixes into 0.4
- Stage 5–6 (chime, theme) and PRD Appendix B unless you move one up

Add more “will not ship” lines as you decide.

-

## Known limits

Carry forward from rc1 unless you fix them. Add new ones as you find
them.

- UI `Start` calls `conveyor start --no-smoke` — CLI smoke still runs
  on direct `conveyor start`
- Intake grade blocks the HTTP request until the one-shot finishes
- Workflow presets live in gitignored `.conveyor/`

-

## M3

May run against **`v0.3.0-rc1`** (frozen) while `main` moves. Classify
surprises in `test/fixtures/m3/`. Fixes you want in 0.4 go to `main`
and get a line under **Fixes** above.

Candidate craft list (not auto-in):
[`.cursor/plans/post-m3_harden_f4bbac3b.plan.md`](../../.cursor/plans/post-m3_harden_f4bbac3b.plan.md).
Copy a todo here if you want it in this tag.
